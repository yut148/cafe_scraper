require 'spec_helper'

describe CafeScraper do
  it 'has a version number' do
    expect(CafeScraper::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
